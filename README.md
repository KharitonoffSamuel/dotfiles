# dotfiles
My own dotfiles configuration
# Dotfiles pour devcontainer

Ce repository contient les configurations personnelles pour zsh, git et vim.

## Installation manuelle

```bash
git clone https://github.com/skh/dotfiles.git ~/dotfiles
cd ~/dotfiles
bash install.sh